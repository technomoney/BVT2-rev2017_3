using UnityEngine;
using UnityEngine.UI;

public class Menu_Options : MonoBehaviour
{
    public Vector2 initialPosition;
    public Button pfb_backgroundButton;
    public float xIncrement, yIncrement;

    // Use this for initialization
    private void Start()
    {
        //assign the background to the buttons	
    }

    // Update is called once per frame
    private void Update()
    {
    }
}